<div class="jumbotron">
  <h1 class="display-4">Hello, Gossipers!</h1>
  <p class="lead">What, wait?!? Do you gossip? Join Us as we discuss some Hot New Gossip </p>
  <hr class="my-4">
  <p>Join our comunity!!! NOW!! XoXo</p>
  <p class="lead">
    <a class="btn btn-outline-dark text-center" <%= link_to 'Créer un Gossip', new_gossip_path %></a>
  </p>
</div>

<body>
  <% @gossips.each do |gossip| %>
    <div class="card">
      <div class="card-body"><%= gossip.title %>
        <blockquote class ="blockquote mb-0">
        <p> <%= gossip.content %> </p>
        <a class="btn btn-outline-dark text-center" <%= link_to 'Voir le gossip', gossip_path(gossip.id) %></a>
      </div>
    </div>
  <% end %>
</body>
